@model List<ChapsDotNET.Business.Models.MPModel>

@{
    ViewData["Title"] = "Members of Parliament administration";
}

<style>
    .flex-container {
        display: flex;
        flex-wrap: wrap;
    }

    .flex-container > form,
    .flex-container > table {
        margin: 15px;
        height: min-content;
    }
</style>

<div class="breadcrumbs">
    <a href="">Home</a>
    &nbsp;>&nbsp;
    <a href="">Administration</a>
</div>

<h1>Members of Parliament (MPs)</h1>

<div class="actionLinks">
    @Html.ActionLink("Add a new member of Parliament", "Create", "MPs")
</div>

<div class="flex-container">
    @using (Html.BeginForm())
    {
        <fieldset>
            <legend class="caption">
                Filter
            </legend>
            <label class="editor-label" for="nameContains">
                Name contains:
            </label>
            <div class="editor-field">
                @Html.TextBox("nameSearchTerm", null)
            </div>

            <label class="editor-label" for="addressSearchTerm">
                Address contains
            </label>
            <div class="editor-field">
                @Html.TextBox("addressContains", null)
            </div>

            <label class="editor-label" for="emailSearchTerm">
                Email address contains
            </label>
            <div class="editor-field">
                @Html.TextBox("emailSearchTerm", null)
            </div>

            @*@Html.TextBox("onlyActive", null)*@
            <input alt="Filter MPs" type="submit" value="Apply filters" />
        </fieldset>
    }

    <table>
        <caption class="caption">Members of Parliament</caption>
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    Address
                </th>
                <th>
                    Email
                </th>
                <th>
                    Active
                </th>
                <th>
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var mp in Model.ToList())
            {
                <tr>
                    <td>
                        <a href="/Admin/MPs/Edit/@mp.MPId">
                            @Html.DisplayFor(modelItem => mp.DisplayFullName)
                        </a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => mp.DisplaySingleLineAddress)
                    </td>
                    <td>
                        @if (!string.IsNullOrWhiteSpace(mp.Email))
                        {
                            @Html.DisplayFor(x => mp.Email)
                        }
                    </td>
                    <td align="center">
                        @Html.DisplayFor(x => mp.Active)
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
